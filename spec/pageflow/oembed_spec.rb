require "spec_helper"

RSpec.describe Pageflow::Oembed do
  it "has a version number" do
    expect(Pageflow::Oembed::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
